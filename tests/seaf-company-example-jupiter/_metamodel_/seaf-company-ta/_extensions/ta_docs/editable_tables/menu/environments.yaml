entities:
  seaf.company.ta.docs.environments.edit_menu:
    menu: >
      (
        $domain_id := "seaf.company.ta";
        $root_menu := $seaf_fn_get_root_menu($."seaf.configs", $domain_id);
        $root_menu_order := $seaf_fn_get_root_menu_order($."seaf.configs", $domain_id);
        $target_menu := $seaf_fn_generate_menu_item("Сервисы/Окружения/Окружения/Изменение окружений", $root_menu);
        [
          {
            "link": "/docs/seaf.company.ta.docs.environments.edit",
            "location": $target_menu,
            "order": $root_menu_order + 197
          }
        ]
      )
