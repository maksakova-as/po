entities:
  seaf.company.ta.docs.dcs.edit_menu:
    menu: >
      (
        $domain_id := "seaf.company.ta";
        $root_menu := $seaf_fn_get_root_menu($."seaf.configs", $domain_id);
        $root_menu_order := $seaf_fn_get_root_menu_order($."seaf.configs", $domain_id);
        $target_menu := $seaf_fn_generate_menu_item("Сервисы/Локации/ЦОДы/Изменение ЦОДы", $root_menu);
        [
          {
            "link": "/docs/seaf.company.ta.docs.dcs.edit",
            "location": $target_menu,
            "order": $root_menu_order + 121
          }
        ]
      )
