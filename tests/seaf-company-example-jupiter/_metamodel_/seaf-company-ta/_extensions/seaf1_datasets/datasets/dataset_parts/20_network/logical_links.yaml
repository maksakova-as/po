# Назначение: Подготовка датасета `seaf.company.ds.ta.logical_links` (logical links) в контуре seaf1_datasets.
# Используется: compat-слой seaf1 -> seaf2 (подключается по необходимости через extension root).
# SEAF1 counterpart of seaf.company.ds.ta.logical_links
datasets:
  seaf.company.ds.ta.logical_links:
    description: |
      Логические связи между объектами
    origin:
      by_entities: seaf.ds.objects_by_entities
      data_lake: ($)
    source: >
      (
        $seaf2 := $exists(by_entities."seaf.company.ta.services.logical_links") ? by_entities."seaf.company.ta.services.logical_links" : {};
        $list := data_lake."seaf.ta.services.logical_link";
        $seaf1 := $exists($list)
          ? $each($list, function($val, $key) {
              {
                $key: $merge([
                  $val,
                  {
                    "__id__": $key,
                    "__entity_id__": "seaf.company.ta.services.logical_links",
                    "__entity_id": $key,
                    "__object_type": "seaf.company.ta.services.logical_links"
                  }
                ])
              }
            }) ~> $merge()
          : {};
        $merge([$seaf2, $seaf1])
      )
