datasets:
  seaf.company.ds.sys_components:
    description: |
      Датасет формирует коллекцию всех объектов

    origin: seaf.ds.objects_by_entities
    source: >
      (
      $."seaf.company.sys_components";
      )
