# Назначение: Подготовка датасета `seaf.company.ds.ta.cluster_virtualizations` (cluster virtualizations) в контуре seaf1_datasets.
# Используется: compat-слой seaf1 -> seaf2 (подключается по необходимости через extension root).
# SEAF1 counterpart of seaf.company.ds.ta.cluster_virtualizations
datasets:
  seaf.company.ds.ta.cluster_virtualizations:
    description: |
      Коллекция кластеров виртуализации
    origin:
      by_entities: seaf.ds.objects_by_entities
      data_lake: ($)
    source: >
      (
        $seaf2 := $exists(by_entities."seaf.company.ta.services.cluster_virtualizations") ? by_entities."seaf.company.ta.services.cluster_virtualizations" : {};
        $list := data_lake."seaf.ta.services.cluster_virtualization";
        $seaf1 := $exists($list)
          ? $each($list, function($val, $key) {
              {
                $key: $merge([
                  $val,
                  {
                    "__id__": $key,
                    "__entity_id__": "seaf.company.ta.services.cluster_virtualizations",
                    "__entity_id": $key,
                    "__object_type": "seaf.company.ta.services.cluster_virtualizations"
                  }
                ])
              }
            }) ~> $merge()
          : {};
        $merge([$seaf2, $seaf1])
      )
