rules:
  validators:
    seaf.validation.ta.components.network_connection_check:
      uid: SEAF-TA-NETWORK-COMP-CONN-001
      title: Проверка корректности связи Сетевого устройства с сетями
      description: |
        Проверяет, что у каждого Сетевого устройства (seaf.company.ta.components.networks)
        указано поле `network_connection`, оно не пустое и каждый его элемент
        ссылается на реально существующую сеть.
      correction: |
        Убедитесь, что для каждого объекта типа `seaf.company.ta.components.networks` указано
        непустое поле `network_connection` и что каждая указанная сеть
        определена в `seaf.company.ta.services.networks`.
      source: |
        (
          $configs := $."seaf.configs";
          $ta_config := (
            $cfg_root := $configs."seaf.configs" ? $configs."seaf.configs" : $configs;
            $cfg1 := $configs."seaf.company.ta";
            $cfg2 := $configs.seaf.company.ta;
            $cfg3 := $cfg_root."seaf.company.ta";
            $cfg4 := $cfg_root.seaf.company.ta;
            $cfg1 ? $cfg1 : ($cfg2 ? $cfg2 : ($cfg3 ? $cfg3 : $cfg4))
          );
          $rules_cfg := $ta_config.rules ? $ta_config.rules : {};
          $rules_enabled := $exists($rules_cfg.rules_enabled) ? $rules_cfg.rules_enabled : true;
          $rules_disabled := $exists($rules_cfg.rules_disabled) ? $rules_cfg.rules_disabled : [];
          $rule_id := "seaf.validation.ta.components.network_connection_check";
          $rule_uid := $self.uid;
          $rule_is_disabled := ($rule_id in $rules_disabled) or ($rule_uid in $rules_disabled);
        
          $rules_enabled and $not($rule_is_disabled)
            ? (
              ([
                (
                  $ds := $.datasets."seaf.company.ds.ta.network_components";
                  $components := $eval($ds.source, { "objects": $, "data_lake": $ });
                  $net_ds := $.datasets."seaf.company.ds.ta.networks";
                  $networks := $eval($net_ds.source, { "objects": $, "data_lake": $ });
                  $each($components, function($component, $component_key) {
                    (
                        $connections := $component.network_connection;
                        $not($exists($connections)) or $count($connections) = 0 ? {
                            "uid": "seaf.validation.ta.network_connection_check.missing." & $component_key,
                            "title": "У Сетевого устройства '" & $component.title & "' отсутствует или не заполнено поле network_connection.",
                            "correction": "ID: " & $component_key & ". " & ("Необходимо указать хотя бы одну сеть для поля `network_connection`."),
                            "description": "У Сетевого устройства '" & $component.title & "' (UID: " & $component_key & ") отсутствует или не заполнено поле `network_connection`."
                        } : (
                            $defined_network_keys := $keys($networks);
                            $invalid_connections := $filter($connections, function($c) { $not($c in $defined_network_keys) });
                            $count($invalid_connections) > 0 ? {
                                "uid": "seaf.validation.ta.network_connection_check.notfound." & $component_key,
                                "title": "Сетевое устройство '" & $component.title & "' содержит ссылку на несуществующую сеть.",
                                "correction": "ID: " & $component_key & ". " & ("Проверьте все сети в поле `network_connection` для устройства '" & $component.title & "'. Одна или несколько из них не определены в `seaf.company.ta.services.networks`."),
                                "description": "Сетевое устройство '" & $component.title & "' (UID: " & $component_key & ") ссылается на несуществующие сети: " & $join($invalid_connections, ", ")
                            }
                        )
                    )
                  })
                )
              ])
            )
            : []
        )
