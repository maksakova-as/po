# Назначение: Подготовка датасета `seaf.company.ds.ta.network_links` (network links) в контуре seaf1_datasets.
# Используется: compat-слой seaf1 -> seaf2 (подключается по необходимости через extension root).
# SEAF1 counterpart of seaf.company.ds.ta.network_links
datasets:
  seaf.company.ds.ta.network_links:
    description: |
      Сетевые связанности
    origin:
      by_entities: seaf.ds.objects_by_entities
      data_lake: ($)
    source: >
      (
        $seaf2 := $exists(by_entities."seaf.company.ta.services.network_links") ? by_entities."seaf.company.ta.services.network_links" : {};
        $list := data_lake."seaf.ta.services.network_links";
        $seaf1 := $exists($list)
          ? $each($list, function($val, $key) {
              {
                $key: $merge([
                  $val,
                  {
                    "__id__": $key,
                    "__entity_id__": "seaf.company.ta.services.network_links",
                    "__entity_id": $key,
                    "__object_type": "seaf.company.ta.services.network_links"
                  }
                ])
              }
            }) ~> $merge()
          : {};
        $merge([$seaf2, $seaf1])
      )
