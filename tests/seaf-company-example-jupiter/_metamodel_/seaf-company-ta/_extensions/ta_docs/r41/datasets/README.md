# R41 datasets

Датасеты схемы R41, упорядоченные по этапам:
- `10_sources` — статические/файловые источники;
- `20_compat` — compat-нормализация для seaf2/seaf1/reverse;
- `30_merge` — финальная сборка plural.

Подключение: только через `root.yaml` -> `dataset_parts/root.yaml`.
