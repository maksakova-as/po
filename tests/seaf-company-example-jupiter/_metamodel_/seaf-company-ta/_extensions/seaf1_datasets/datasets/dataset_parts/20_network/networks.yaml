# Назначение: Подготовка датасета `seaf.company.ds.ta.networks` (networks) в контуре seaf1_datasets.
# Используется: compat-слой seaf1 -> seaf2 (подключается по необходимости через extension root).
# SEAF1 counterpart of seaf.company.ds.ta.networks
datasets:
  seaf.company.ds.ta.networks:
    description: |
      Коллекция LAN и WAN сетей
    origin:
      by_entities: seaf.ds.objects_by_entities
      data_lake: ($)
    source: >
      (
        $seaf2 := $exists(by_entities."seaf.company.ta.services.networks") ? by_entities."seaf.company.ta.services.networks" : {};
        $list := data_lake."seaf.ta.services.network";
        $seaf1 := $exists($list)
          ? $each($list, function($val, $key) {
              {
                $key: $merge([
                  $val,
                  {
                    "__id__": $key,
                    "__entity_id__": "seaf.company.ta.services.networks",
                    "__entity_id": $key,
                    "__object_type": "seaf.company.ta.services.networks"
                  }
                ])
              }
            }) ~> $merge()
          : {};
        $merge([$seaf2, $seaf1])
      )
