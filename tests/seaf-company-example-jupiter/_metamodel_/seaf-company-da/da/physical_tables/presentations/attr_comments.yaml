entities:
  seaf.company.da.physical_tables:

    presentations:
      # Встраиваемся в презентацию карточки
      attr_card:
        widgets:
          attr_comments:
            title: Комментарии
            # Идентификатор презентации, которая будет использоваться в виджете
            presentation: attr_comments
            # Расположение
            align: "v"
            # Порядок расположения
            order: 100
            # Определяем стиль виджета
            style:
              margin: 4px
              padding: 0px
              border: true

      # Презентация для виджета
      attr_comments:
        params:
          type: object
          properties:
            id:
              title: Идентификатор объекта
              type: string
            attr_id:
              title: ID атрибута
              type: string
          required:
            - id
            - attr_id

        type: table
        headers:
          - value: number
            text: №
          - value: comment
            text: Комментарий

        origin:
          objects: seaf.company.ds.da.physical_tables
          all_objects: seaf.ds.objects
        source: >
          (
          $objects := objects;
          $all_objects := all_objects;
          $object_id := $params.id;
          $attr_id := $params.attr_id;
          $obj2present := $lookup($objects, $object_id);
          $attr2present := $obj2present.attributes.$lookup($attr_id);
          $attr2present.comments#$i.{
                                      "number": $i+1,
                                      "comment": $
                                   }[];
          )