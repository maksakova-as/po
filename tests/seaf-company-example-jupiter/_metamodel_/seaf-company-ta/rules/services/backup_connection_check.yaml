rules:
  validators:
    seaf.validation.ta.services.backup_connection_check:
      uid: SEAF-TA-BACKUP-CONN-001
      title: Проверка корректности связи Сервиса резервного копирования с сетями
      description: |
        Проверяет, что у каждого Сервиса резервного копирования (seaf.company.ta.services.backups)
        указано поле `network_connection`, оно не пустое и каждый его элемент
        ссылается на реально существующую сеть.
      correction: |
        Убедитесь, что для каждого объекта типа `seaf.company.ta.services.backups` указано
        непустое поле `network_connection` и что каждая указанная сеть
        определена в `seaf.company.ta.services.networks`.
      source: |
        (
          $configs := $."seaf.configs";
          $ta_config := (
            $cfg_root := $configs."seaf.configs" ? $configs."seaf.configs" : $configs;
            $cfg1 := $configs."seaf.company.ta";
            $cfg2 := $configs.seaf.company.ta;
            $cfg3 := $cfg_root."seaf.company.ta";
            $cfg4 := $cfg_root.seaf.company.ta;
            $cfg1 ? $cfg1 : ($cfg2 ? $cfg2 : ($cfg3 ? $cfg3 : $cfg4))
          );
          $rules_cfg := $ta_config.rules ? $ta_config.rules : {};
          $rules_enabled := $exists($rules_cfg.rules_enabled) ? $rules_cfg.rules_enabled : true;
          $rules_disabled := $exists($rules_cfg.rules_disabled) ? $rules_cfg.rules_disabled : [];
          $rule_id := "seaf.validation.ta.services.backup_connection_check";
          $rule_uid := $self.uid;
          $rule_is_disabled := ($rule_id in $rules_disabled) or ($rule_uid in $rules_disabled);
          $cfg_root2 := $configs."seaf.configs" ? $configs."seaf.configs" : $configs;
          $company_ids := $keys($cfg_root2.company ? $cfg_root2.company : ($configs.company ? $configs.company : {}));
          $has_company_filter := $count($company_ids) > 0;
          $is_local := function($obj, $key){
            $has_company_filter
              ? (
                  ($exists($obj.__company_id__) and $obj.__company_id__ in $company_ids)
                  or ($substringBefore($key, ".") in $company_ids)
                )
              : (
                  $not($exists($obj.source)) or $obj.source = "seaf2"
                )
          };
        
          $rules_enabled and $not($rule_is_disabled)
            ? (
              ([
                (
                  $ds := $.datasets."seaf.company.ds.ta.backup";
                  $entities := $eval($ds.source, { "objects": $, "data_lake": $ });
                  $net_ds := $.datasets."seaf.company.ds.ta.networks";
                  $networks := $eval($net_ds.source, { "objects": $, "data_lake": $ });
                  $each($entities, function($entity, $entity_key) {
                    (
                        $not($is_local($entity, $entity_key)) ? [] :
                        $connections := $entity.network_connection;
                        $not($exists($connections)) or $count($connections) = 0 ? {
                            "uid": "seaf.validation.ta.backup_connection_check.missing." & $entity_key,
                            "title": "У Сервиса резервного копирования '" & $entity.title & "' отсутствует или не заполнено поле network_connection.",
                            "correction": "ID: " & $entity_key & ". " & ("Необходимо указать хотя бы одну сеть для поля `network_connection`."),
                            "description": "У Сервиса резервного копирования '" & $entity.title & "' (UID: " & $entity_key & ") отсутствует или не заполнено поле `network_connection`."
                        } : (
                            $defined_network_keys := $keys($networks);
                            $invalid_connections := $filter($connections, function($c) { $not($c in $defined_network_keys) });
                            $count($invalid_connections) > 0 ? {
                                "uid": "seaf.validation.ta.backup_connection_check.notfound." & $entity_key,
                                "title": "Сервис резервного копирования '" & $entity.title & "' содержит ссылку на несуществующую сеть.",
                                "correction": "ID: " & $entity_key & ". " & ("Проверьте все сети в поле `network_connection` для сущности '" & $entity.title & "'. Одна или несколько из них не определены в `seaf.company.ta.services.networks`."),
                                "description": "Сервис резервного копирования '" & $entity.title & "' (UID: " & $entity_key & ") ссылается на несуществующие сети: " & $join($invalid_connections, ", ")
                            }
                        )
                    )
                  })
                )
              ])
            )
            : []
        )
