# Назначение: Подготовка датасета `seaf.company.ds.ta.dc_offices` (dc offices) в контуре seaf1_datasets.
# Используется: compat-слой seaf1 -> seaf2 (подключается по необходимости через extension root).
# SEAF1 counterpart of seaf.company.ds.ta.dc_offices
datasets:
  seaf.company.ds.ta.dc_offices:
    description: |
      Коллекция офисов и точек присутствия компании
    origin:
      by_entities: seaf.ds.objects_by_entities
      data_lake: ($)
    source: >
      (
        $seaf2 := $exists(by_entities."seaf.company.ta.services.dc_offices") ? by_entities."seaf.company.ta.services.dc_offices" : {};
        $list := data_lake."seaf.ta.services.office";
        $seaf1 := $exists($list)
          ? $each($list, function($val, $key) {
              {
                $key: $merge([
                  $val,
                  {
                    "__id__": $key,
                    "__entity_id__": "seaf.company.ta.services.dc_offices",
                    "__entity_id": $key,
                    "__object_type": "seaf.company.ta.services.dc_offices"
                  }
                ])
              }
            }) ~> $merge()
          : {};
        $merge([$seaf2, $seaf1])
      )
