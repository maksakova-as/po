rules:
  validators:
    seaf.validation.ta.environments.environment_existence:
      uid: SEAF-TA-EXIST-ENVIRONMENT
      title: Проверка наличия Окружения (environment)
      description: |
        Проверяет, что в архитектуре описано хотя бы одно Окружение (seaf.company.ta.services.environment).
      correction: |
        Убедитесь, что в файлах архитектуры (`/architecture/ta/`) создан
        хотя бы один объект типа `seaf.company.ta.services.environment`.
      source: |
        (
          $configs := $."seaf.configs";
          $ta_config := (
            $cfg_root := $configs."seaf.configs" ? $configs."seaf.configs" : $configs;
            $cfg1 := $configs."seaf.company.ta";
            $cfg2 := $configs.seaf.company.ta;
            $cfg3 := $cfg_root."seaf.company.ta";
            $cfg4 := $cfg_root.seaf.company.ta;
            $cfg1 ? $cfg1 : ($cfg2 ? $cfg2 : ($cfg3 ? $cfg3 : $cfg4))
          );
          $rules_cfg := $ta_config.rules ? $ta_config.rules : {};
          $rules_enabled := $exists($rules_cfg.rules_enabled) ? $rules_cfg.rules_enabled : true;
          $rules_disabled := $exists($rules_cfg.rules_disabled) ? $rules_cfg.rules_disabled : [];
          $rule_id := "seaf.validation.ta.environments.environment_existence";
          $rule_uid := $self.uid;
          $rule_is_disabled := ($rule_id in $rules_disabled) or ($rule_uid in $rules_disabled);
        
          $rules_enabled and $not($rule_is_disabled)
            ? (
              ([
                $count($.datasets."seaf.company.ds.ta.environments") = 0 ? {
                    "uid": "seaf.validation.ta.existence.environment",
                    "title": "Отсутствует описание Окружения (environment)",
                    "correction": "ID: " & "seaf.validation.ta.existence.environment" & ". " & ("Необходимо описать хотя бы одно Окружение (seaf.company.ta.services.environment) в архитектуре."),
                    "description": "В архитектуре не описано ни одного Окружения."
                }
              ])
            )
            : []
        )
