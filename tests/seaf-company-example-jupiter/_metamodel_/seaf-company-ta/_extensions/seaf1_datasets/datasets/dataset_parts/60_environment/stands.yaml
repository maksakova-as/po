# Назначение: Подготовка датасета `seaf.company.ds.ta.stands` (stands) в контуре seaf1_datasets.
# Используется: compat-слой seaf1 -> seaf2 (подключается по необходимости через extension root).
# SEAF1 counterpart of seaf.company.ds.ta.stands
datasets:
  seaf.company.ds.ta.stands:
    description: |
      Стенды окружений
    origin:
      by_entities: seaf.ds.objects_by_entities
      data_lake: ($)
    source: >
      (
        $seaf2 := $exists(by_entities."seaf.company.ta.services.stands") ? by_entities."seaf.company.ta.services.stands" : {};
        $list := data_lake."seaf.ta.services.stand";
        $seaf1 := $exists($list)
          ? $each($list, function($val, $key) {
              {
                $key: $merge([
                  $val,
                  {
                    "__id__": $key,
                    "__entity_id__": "seaf.company.ta.services.stands",
                    "__entity_id": $key,
                    "__object_type": "seaf.company.ta.services.stands"
                  }
                ])
              }
            }) ~> $merge()
          : {};
        $merge([$seaf2, $seaf1])
      )
