entities:
  seaf.company.ta.docs.backup.edit_menu:
    menu: >
      (
        $domain_id := "seaf.company.ta";
        $root_menu := $seaf_fn_get_root_menu($."seaf.configs", $domain_id);
        $root_menu_order := $seaf_fn_get_root_menu_order($."seaf.configs", $domain_id);
        $target_menu := $seaf_fn_generate_menu_item("Сервисы/Инфраструктура/Резервное копирование/Изменение резервного копирования", $root_menu);
        [
          {
            "link": "/docs/seaf.company.ta.docs.backup.edit",
            "location": $target_menu,
            "order": $root_menu_order + 192
          }
        ]
      )
