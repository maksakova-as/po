# Доработки модуля

  - [x] Ошибочно обрабатыванется секция required в конструкции anyOf\allOf\oneOf - интерпретируется как отдельный атрибут
  - [x] Сделать корректную обработку секции oneOf - помечать альернативные варианты реализации разными названиями атрибута\связи
  - [x] Иногда атрибут типа link интерпретируется как атрибут типа value (при использовании секции oneOf внутри)
  - [x] Для некорневых элементов patternProperties - если элемент 1, то не нумеровать: __key__
  - [x] Переделать алгоритм получения списка сущностей с атрибутами в связи с изменением алгоритма $resolve_refs
  - [x] Секции anyOf (allOf) схлопываются и не показывают все варианты - например когда связь описывается с разными сущностями - показывается только последняя
  - [ ] В поле Description jsonschema -  вместо > использовать | - в vscode подсказки более читательные становятся

 