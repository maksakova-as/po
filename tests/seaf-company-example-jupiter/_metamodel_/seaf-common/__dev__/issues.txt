# Бэклог модуля

## Общее
 -[x] в seaf.ds.objects добавить домен (компанию) __company_id__
 -[x] в seaf.ds.objects добавить ссылку на объект __link__
 -[ ] Применить подход докхаба - для каждой сущности в ее конфиге хранить entities.xxx.config.defaultPresentation - чтобы использовать по-умолчанию в карточках
 -[ ] Для каждой сущности прописать настройку - entities.xxx.config.company_id_segments_number - брать в первую очередь ее, а потом уже читать глоб. настройку
 -[ ] Описать чек-лист - что нужно настроить\внести при создании нового модуля:
    - описать схему энетити
    - сделать настройки датасетов seaf.ds.entities, seaf.ds.objects_links
    - задекларировать фейковые сущности для внешних зависимостей (TBD)

## Портальный режим работы
Для того чтобы в меню работали функции при включенной ролевой модели необходимо:
добавить в default.yaml (файл с дефолтными регулярками для всех ролей) строки
 - "^entities$"
 - "^functions$"
 - "^seaf.*$"

## Типовая адаптация модулей seaf-company для работы в составе seafera-portal (сводный репо Группы Сбер)
 - адапатировать меню
 - презентации должны иметь вх. параметр domain - чтобы портал мог строить списки в разрезе компаний. если парамтер не задан, то показываем все имеющиеся данные (не фильтруем)
 - seaf.configs




 Решить - нужны ли датасеты:
 #  seaf.ds.modules_dependencies:
 #    title: DS зависимости модулей
 #    description: >
 #      Датасет показывает список модулей, от которых зависит данный репозиторий.
 #    source: >
 #      ($distinct(**.dependencies~>$spread()))

 #  seaf.ds.defs:
 #    title: DS определения defs
 #    description: >
 #      Коллекция всех defs (элементов jsonschema) метамодели.
 #      Поиск объекта через функцию lookup, например: $.$lookup("имя_объекта")
 #    source: >
 #      (
 #      $defs := $merge($.entities.*.schema."$defs");
 #      $arr_defs := $each($defs, function($v, $k){
 #          $merge([{"__id__": $k }, $v])
 #      });
 #      $arr_defs.{__id__: $}~>$merge()
 #      )
