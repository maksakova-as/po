# Назначение: Подготовка датасета `seaf.company.ds.ta.dcs` (dcs) в контуре seaf1_datasets.
# Используется: compat-слой seaf1 -> seaf2 (подключается по необходимости через extension root).
# SEAF1 counterpart of seaf.company.ds.ta.dcs
datasets:
  seaf.company.ds.ta.dcs:
    description: |
      Коллекция ЦОДов (Datacenter) компании
    origin:
      by_entities: seaf.ds.objects_by_entities
      data_lake: ($)
    source: >
      (
        $seaf2 := $exists(by_entities."seaf.company.ta.services.dcs") ? by_entities."seaf.company.ta.services.dcs" : {};
        $list := data_lake."seaf.ta.services.dc";
        $seaf1 := $exists($list)
          ? $each($list, function($val, $key) {
              {
                $key: $merge([
                  $val,
                  {
                    "__id__": $key,
                    "__entity_id__": "seaf.company.ta.services.dcs",
                    "__entity_id": $key,
                    "__object_type": "seaf.company.ta.services.dcs"
                  }
                ])
              }
            }) ~> $merge()
          : {};
        $merge([$seaf2, $seaf1])
      )
