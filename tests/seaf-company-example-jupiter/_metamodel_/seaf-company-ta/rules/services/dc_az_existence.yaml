rules:
  validators:
    seaf.validation.ta.services.dc_az_existence:
      uid: SEAF-TA-EXIST-003
      title: Проверка наличия Зоны доступности (dc_az)
      description: |
        Проверяет, что в архитектуре описана хотя бы одна Зона доступности (seaf.company.ta.services.dc_azs).
      correction: |
        Убедитесь, что в файлах архитектуры (`/architecture/ta/`) создан
        хотя бы один объект типа `seaf.company.ta.services.dc_azs`.
      source: |
        (
          $configs := $."seaf.configs";
          $ta_config := (
            $cfg_root := $configs."seaf.configs" ? $configs."seaf.configs" : $configs;
            $cfg1 := $configs."seaf.company.ta";
            $cfg2 := $configs.seaf.company.ta;
            $cfg3 := $cfg_root."seaf.company.ta";
            $cfg4 := $cfg_root.seaf.company.ta;
            $cfg1 ? $cfg1 : ($cfg2 ? $cfg2 : ($cfg3 ? $cfg3 : $cfg4))
          );
          $rules_cfg := $ta_config.rules ? $ta_config.rules : {};
          $rules_enabled := $exists($rules_cfg.rules_enabled) ? $rules_cfg.rules_enabled : true;
          $rules_disabled := $exists($rules_cfg.rules_disabled) ? $rules_cfg.rules_disabled : [];
          $rule_id := "seaf.validation.ta.services.dc_az_existence";
          $rule_uid := $self.uid;
          $rule_is_disabled := ($rule_id in $rules_disabled) or ($rule_uid in $rules_disabled);
        
          $rules_enabled and $not($rule_is_disabled)
            ? (
              ([
                (
                  $data := $."seaf.company.ta.services.dc_azs";
                  $count($data) = 0 ? {
                      "uid": "seaf.validation.ta.existence.dc_az",
                      "title": "Отсутствует описание Зоны доступности (dc_az)",
                      "correction": "ID: " & "seaf.validation.ta.existence.dc_az" & ". " & ("Необходимо описать хотя бы одну Зону доступности (seaf.company.ta.services.dc_azs) в архитектуре."),
                      "description": "В архитектуре не описано ни одной Зоны доступности. Это базовый элемент для построения иерархии ТА."
                  }
                )
              ])
            )
            : []
        )
