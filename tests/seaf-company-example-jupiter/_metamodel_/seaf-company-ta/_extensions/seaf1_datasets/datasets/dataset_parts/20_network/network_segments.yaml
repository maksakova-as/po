# Назначение: Подготовка датасета `seaf.company.ds.ta.network_segments` (network segments) в контуре seaf1_datasets.
# Используется: compat-слой seaf1 -> seaf2 (подключается по необходимости через extension root).
# SEAF1 counterpart of seaf.company.ds.ta.network_segments
datasets:
  seaf.company.ds.ta.network_segments:
    description: |
      Коллекция сетевых сегментов
    origin:
      by_entities: seaf.ds.objects_by_entities
      data_lake: ($)
    source: >
      (
        $seaf2 := $exists(by_entities."seaf.company.ta.services.network_segments") ? by_entities."seaf.company.ta.services.network_segments" : {};
        $list := data_lake."seaf.ta.services.network_segment";
        $seaf1 := $exists($list)
          ? $each($list, function($val, $key) {
              {
                $key: $merge([
                  $val,
                  {
                    "__id__": $key,
                    "__entity_id__": "seaf.company.ta.services.network_segments",
                    "__entity_id": $key,
                    "__object_type": "seaf.company.ta.services.network_segments"
                  }
                ])
              }
            }) ~> $merge()
          : {};
        $merge([$seaf2, $seaf1])
      )
