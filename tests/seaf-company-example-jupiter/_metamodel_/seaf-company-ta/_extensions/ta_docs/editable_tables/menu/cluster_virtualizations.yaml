entities:
  seaf.company.ta.docs.cluster_virtualizations.edit_menu:
    menu: >
      (
        $domain_id := "seaf.company.ta";
        $root_menu := $seaf_fn_get_root_menu($."seaf.configs", $domain_id);
        $root_menu_order := $seaf_fn_get_root_menu_order($."seaf.configs", $domain_id);
        $target_menu := $seaf_fn_generate_menu_item("Сервисы/Платформа/Кластеры виртуализации/Изменение кластеров виртуализации", $root_menu);
        [
          {
            "link": "/docs/seaf.company.ta.docs.cluster_virtualizations.edit",
            "location": $target_menu,
            "order": $root_menu_order + 181
          }
        ]
      )
