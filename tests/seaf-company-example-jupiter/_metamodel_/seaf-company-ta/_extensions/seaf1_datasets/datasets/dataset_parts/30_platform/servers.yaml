# Назначение: Подготовка датасета `seaf.company.ds.ta.servers` (servers) в контуре seaf1_datasets.
# Используется: compat-слой seaf1 -> seaf2 (подключается по необходимости через extension root).
# SEAF1 counterpart of seaf.company.ds.ta.servers
datasets:
  seaf.company.ds.ta.servers:
    description: |
      Коллекция физических и виртуальных серверов
    origin:
      by_entities: seaf.ds.objects_by_entities
      data_lake: ($)
    source: >
      (
        $seaf2 := $exists(by_entities."seaf.company.ta.components.servers") ? by_entities."seaf.company.ta.components.servers" : {};
        $list := data_lake."seaf.ta.components.server";
        $seaf1 := $exists($list)
          ? $each($list, function($val, $key) {
              {
                $key: $merge([
                  $val,
                  {
                    "__id__": $key,
                    "__entity_id__": "seaf.company.ta.components.servers",
                    "__entity_id": $key,
                    "__object_type": "seaf.company.ta.components.servers"
                  }
                ])
              }
            }) ~> $merge()
          : {};
        $merge([$seaf2, $seaf1])
      )
