entities:
  seaf.company.da.logical_links:

    menu: >
      (
          $entity_id := "seaf.company.da.logical_links";
          $domain_id := "seaf.company.da";
          $root_menu := $seaf_fn_get_root_menu($."seaf.configs", $domain_id);
          $root_menu_order := $seaf_fn_get_root_menu_order($."seaf.configs", $domain_id) + 500;
          $menu := $seaf_fn_generate_menu_item("Логическая Модель Данных/Связи", $root_menu);
          [
          $exists($.$lookup($entity_id)) 
          ?   {
              "link": "/entities/" & $entity_id & "/list",
              "location": $menu,
              "order": $root_menu_order
              }
          ]  
      )
